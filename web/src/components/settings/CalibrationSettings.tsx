// CalibrationSettings is a thin wrapper around CalibrationPage.
// It exists to maintain the settings component folder convention and
// allows future settings-specific wrappers (e.g. permissions guards).
export { CalibrationPage as CalibrationSettings } from '../../pages/calibration/CalibrationPage';
